# 环境变量配置模板
# A股量化选股器 - 环境变量配置示例
# 请复制此文件为.env并根据实际情况修改配置

# 应用运行端口
PORT=3000

# JWT认证配置（必须设置强密钥）
JWT_SECRET= # 必须设置！请使用强密钥，长度建议32位以上
JWT_EXPIRES_IN=24h # JWT令牌过期时间

# 数据来源配置
USE_MOCK_DATA=true # 设置为true使用本地模拟数据，false使用真实API数据
MOCK_DATA_FILE=data/processed/stock_data_strategy_test.json # 模拟数据文件路径

# Tushare API配置（当USE_MOCK_DATA为false时生效）
TUSHARE_API_KEY= # Tushare API密钥
TUSHARE_RETRY_TIMES=3 # API请求重试次数
TUSHARE_TIMEOUT=10000 # API请求超时时间（毫秒）

# 缓存配置
CACHE_ENABLED=true # 是否启用缓存
CACHE_EXPIRE_TIME=86400000 # 缓存过期时间（毫秒），默认为24小时
CACHE_STORAGE_KEY=stock_data_cache # 缓存存储键名

# 超级管理员配置
SUPER_ADMIN_PHONES=18066668888 # 超级管理员手机号，多个用逗号分隔
SUPER_ADMIN_UNLIMITED=true # 超级管理员是否无使用限制

# 开发环境配置
NODE_ENV=development # 设置为development或production
DEBUG=false # 是否启用调试模式
VERBOSE_LOGGING=false # 是否显示详细日志

# 安全提示：
# 1. 请务必设置JWT_SECRET为强密钥，不要使用默认值
# 2. 在生产环境中，不要将敏感信息提交到代码仓库
# 3. 定期更新API密钥和认证密钥
# 4. 不要在客户端暴露服务器端环境变量
# 5. 生产环境中NODE_ENV应设置为production